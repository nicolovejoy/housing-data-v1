# Infrastructure Configuration

Terraform configuration for deploying to Railway.

## Files

- `main.tf` - Main infrastructure definition
- `variables.tf` - Input variables
- `outputs.tf` - Output values

## Usage

```bash
terraform init
terraform plan
terraform apply
```
