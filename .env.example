# =============================================================================
# HUD Fair Market Rent Data Explorer - Environment Configuration
# =============================================================================

# =============================================================================
# HUD API Configuration (for data fetching script)
# =============================================================================
# Get your API token at: https://www.huduser.gov/portal/dataset/fmr-api.html
# Instructions:
# 1. Create a free account at the HUD API page above
# 2. Copy your API token
# 3. Create a .env file (copy this file and rename it)
# 4. Replace 'your_api_token_here' with your actual token
# 5. NEVER commit the .env file to version control

HUD_API_KEY=your_api_token_here

# =============================================================================
# Frontend Configuration
# =============================================================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# =============================================================================
# Backend / Database Configuration
# =============================================================================
# PostgreSQL connection
DATABASE_URL=postgresql://user:password@localhost:5432/fmr_data

# Database credentials (used if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fmr_data

# Node environment
NODE_ENV=development

# =============================================================================
# Railway Deployment Configuration
# =============================================================================
# These are set automatically by Railway, but can be overridden:

# RAILWAY_ENVIRONMENT_NAME=production
# RAILWAY_PROJECT_NAME=housing-data-v1

# =============================================================================
# Optional: Feature Flags
# =============================================================================
# Enable debug logging
DEBUG=false
